## 2026-09-10 — Custom subagent types aren't invocable by name in this environment

`.claude/agents/ui-implementer.md` and `.claude/agents/ui-verifier.md` exist and are well-formed, but this session's Agent tool rejects `subagent_type: "ui-implementer"`/`"ui-verifier"` outright ("Agent type not found — available agents: claude, claude-code-guide, Explore, general-purpose, Plan, statusline-setup"). Custom project-level agents aren't being picked up as distinct dispatchable types here, whatever the reason (environment/harness difference).

**Workaround, in effect from Phase 4 onward**: dispatch `general-purpose` instead, with the relevant `.claude/agents/*.md` file's full role definition folded directly into the prompt (told to read that file and follow it as its standing instructions, plus the task-specific brief). This reproduces the intended implement→verify architecture — an implementer that doesn't verify its own work, a verifier that can't edit and didn't write the code — just via a different dispatch mechanism than originally planned. The two agent-definition files stay in the repo as the source of truth for what each role should do; they're just not directly invocable by name here. If a future session finds custom agent types *do* work (e.g. run via a different Claude Code surface), prefer dispatching them by name instead of this workaround.

# Redesign — standing decisions

Running log of settled calls, so later tasks don't relitigate them. Add new entries at the top with a date.

## 2026-09-10 — Phase 2 infra baseline measured; corrects the 2026-09-09 Neon free-tier assumption

Full numbers in `02-infra-baseline.md`. Headline finding: **Vercel is the dominant cost (~$40/period), not Neon/Upstash/Cloudinary (~$6.59 combined)** — its two biggest line items (Web Analytics Events $10.71/period; Fluid Active CPU $16.81/period alongside 8.26M function invocations) map directly onto two already-approved `02-proposals.md` Phase 6 items (drop `@vercel/analytics`; server-components/redundant-fetch audit), turning those from "good practice" into "confirmed, quantified savings." No provider swap looks warranted — execute the already-approved Phase 6 items and re-measure before considering one.

**Correction**: the "Redis (Upstash)" decision entry from 2026-09-09 (below) separately assumed Neon's database branching was on a *free* tier ("up to 10 branches/project on the free tier"). The account is actually on Neon's **Launch** plan (paid, usage-based — $6.59/period currently: 62.45 compute hours + 0.04 GB-month storage). This doesn't change the branching decision itself (the `redesign` branch's marginal cost is still close to zero — copy-on-write means it shares storage with `main` until schemas diverge, confirmed by both branches measuring identically at 111 MB), but any future session reading the 2026-09-09 entry should read "free tier" there as superseded by this correction.

## 2026-09-10 — International account phone numbers (supersedes part of the auth review below)

User raised a real gap after the fact: some customers plausibly hold non-Egyptian numbers (Gulf-based Egyptian expats buying for family in Egypt is a common pattern in this market), so unconditionally restricting phone to Egypt-only would turn away real customers, not just close a misleading UI element.

- **Split by phone field, not a blanket policy.** The app has two distinct phone fields: the **account/login identity phone** (registration, login, password-reset OTP) and the **delivery-address phone** (per saved address, independently validated already, called by the courier partner to coordinate physical delivery). Decision: the account phone now accepts international numbers; the delivery-address phone **stays Egypt-only** — that field is a courier-reachability requirement (Egypt Post Wasalha needs a number they can actually call during delivery in Egypt), not a login preference, and the user explicitly did not want to loosen it.
- **This *reverses* the "restrict the country dropdown to Egypt only" fix decided in the auth review immediately below.** That fix is superseded — do not implement it. The 22-country dropdown on login/register/forgot-password instead becomes fully real: each country validates via `libphonenumber-js` against its own country code, not hardcoded to `"EG"`. Low engineering lift since `libphonenumber-js` (already a dependency, via `normalizeEgyptMobilePhone`'s use of its `"EG"` mobile parser) supports arbitrary countries out of the box — this is swapping a hardcoded country argument for the user's dropdown selection, plus a corresponding relaxation of `lib/phone.ts`'s account-phone validator (the delivery-address phone helper stays exactly as it is).
- **Country coverage: all 22 already-listed countries**, not a curated subset — user's explicit choice, to start simple since the dropdown UI already exists as-is.
- **Operational dependency, not code**: Twilio blocks SMS to many countries by default for fraud prevention until "Geographic Permissions" are explicitly enabled per destination in the Twilio console. Since OTP is now forgot-password-only (see the auth review below — OTP was removed from login/register entirely), this only affects the password-reset flow's SMS volume, which is relatively low. **User action required before this can work in production**: check/enable Geographic Permissions in the Twilio console for whichever of the 22 countries are expected to see real signups.
- Downstream implications to carry into the `(auth)` Phase 4 task: `User.phone` storage moves from an implicitly-Egypt-shaped string to genuine E.164 international format; phone-uniqueness constraint logic is unaffected (still unique regardless of country); the login/register/forgot-password forms' existing country-code `<select>` needs no UI change, only its backing validation.

## 2026-09-10 — Public storefront/auth feature review

Full read-through of all auth (3) and public storefront (14) feature-inventory files, done specifically to answer "does the storefront/auth need anything added." Decisions below; the "Approved"/"Rejected" items are also mirrored in `02-proposals.md`.

- **PDP image gallery, back-in-stock notifications, and the widened order-history scope (detail page + pagination + reorder)** — all approved, see `02-proposals.md` for specifics.
- **COD fee display** — user explicitly kept the current fold-into-shipping display rather than splitting it into its own honest line item. No change to checkout's pricing display.
- **Smaller correctness fixes, decided under PM latitude** (not asked, corrections with no real trade-off):
  - Auth: `register` and `forgot-password` get the same "redirect an already-logged-in visitor away" guard `login` already has (`login.md` found this asymmetry).
  - ~~Auth: the 22-country phone dropdown on all three auth screens gets restricted to Egypt only~~ — **superseded same-day, see the "International account phone numbers" entry above.** The dropdown instead becomes fully functional across all 22 countries for the account/login phone; only the separate delivery-address phone stays Egypt-only.
  - `home.md`: the home page's "loading skeleton" branch only ever fires on a genuine backend failure (the page is fully server-rendered, so there's no real client-side loading state to show) — replacing it with an honest error message so a real outage doesn't read as "the site is just slow."
  - `products-pdp.md`: product-tag chips all link to the same generic `/products` URL regardless of which tag was clicked, despite `?section=` (the tag filter they're supposed to trigger) already working server-side — wiring the chips to the param they're supposed to use.
  - `cart.md`: item removals (inactive product, or a governorate change pruning unavailable lines) currently only surface as a toast on whichever page the shopper happens to land on next — showing it immediately on the cart page itself when it happens there.
  - `checkout.md`: the `INVALID_CHECKOUT` error currently collapses three distinct root causes (empty cart / a product missing weight data / an unmappable shipping address) into one message — splitting it into three specific, diagnosable messages. This does not change checkout's success-path behavior, only which error a shopper/support agent sees on failure.
  - `become-a-partner.md` and `register.md`: both collect PII with no link to `/terms`/`/privacy` anywhere on the form — adding the links. `become-a-partner.md` additionally has no rate limiting or CAPTCHA on a public POST endpoint — adding basic rate limiting (reusing the existing OTP-limiter pattern).
  - `profile-addresses.md` and `checkout.md`: `building`/`floor`/`apartment` exist on the `SavedAddress` model and are editable via the admin's PATCH API, but **no customer-facing form anywhere** (profile addresses, checkout's new-address form) lets a shopper actually set them — adding these fields to both customer-facing address forms, since they matter for delivery accuracy in Egypt.
- **Deliberately not pursued, noted rather than silently dropped**: a self-service phone-number-change flow on `profile-account.md` (phone is permanently read-only today, no in-app path to change it) — lower frequency need, judged not worth the added verification complexity (would need its own OTP-based re-verification step) for this round. Can be revisited later if it becomes a real support burden.

## 2026-09-10 — Admin/partner dashboard feature review

Full read-through of all 14 admin and 8 partner feature-inventory files, done specifically to answer "does admin/partner need anything added" per user request. Decisions below; the "Approved" items are also mirrored in `02-proposals.md`.

- **Partner dashboard, admin-editable shipping rates, admin partner-edit UI, admin revoke-admin, coupon validity window, stock-transfer confirmation, and agent-side distributor-stock visibility on restock review** — all approved as new-feature/gap-close proposals, see `02-proposals.md` for each item's specifics. Shipping rates specifically must launch seeded with today's exact hardcoded rate constants as defaults, per explicit user instruction — this is a rewire-then-expose change, not a blank-slate rate table.
- **Order/RoutedOrder status propagation** — user pushed back on the initial framing (calling the two models "two independent state machines that can diverge" undersold that routing state is *supposed* to be orthogonal to commercial state mid-flight, e.g. CONFIRMED+OUT_FOR_DELIVERY is normal, not a bug) and asked what the actual fix should be. PM recommendation, stated to the user, not yet explicitly confirmed back: keep the two models separate (routing genuinely needs its own pipeline vocabulary), but auto-propagate at the two edges where contradiction is nonsensical — (1) `Order.status → CANCELLED` auto-cancels the linked `RoutedOrder`; (2) `RoutedOrder.status → DELIVERED` auto-marks the linked `Order` DELIVERED (routing delivery becomes the source of truth for commercial delivery, not a separate manual click on `/admin/orders`). Every other combination is left alone, plus both statuses get surfaced side-by-side wherever either currently appears, as a visibility net for anything not covered by the two auto-propagation rules. **User confirmed** — settled, mirrored as Approved in `02-proposals.md`.
- **Smaller correctness fixes, decided under PM latitude** (not asked, since these are corrections with no real trade-off — noted so they can be challenged):
  - `admin/partners.md`: a partner request's status can be manually set to APPROVED via the plain "حالة" dialog without ever going through "قبول وتحويل" (convert), leaving it stuck approved-but-never-converted with no recovery path. Fix: exclude APPROVED from that dropdown's freely-selectable options — APPROVED should only be reachable via the convert action, which is the only path that actually creates the `Partner` record.
  - `admin/routed-orders.md`: reassigning a routed order silently resets its status back to `ASSIGNED` even if it was already DELIVERED/FAILED/CANCELLED. Fix: block reassignment (or require an explicit extra confirmation) when the current status is one of those three terminal states.
  - `partner/restock-requests.md`: a distributor with no linked agent gets a raw, untranslated English error string ("Distributor has no linked source agent") in an otherwise all-Arabic flow. Fix: give it a proper Arabic message.

## 2026-09-10 — Phase 0 findings triage

Decisions made against the findings surfaced in `00-feature-inventory/` (see its README's "Phase 0 status" section for the full write-up of each finding).

- **Production bug fixes (open-redirect on register, no rate limiting on password login/register, InstaPay account keyed by partner-name string match) are fixed inside the redesign, not on `main`.** User's explicit call, overriding the PM's initial recommendation to patch `main` immediately. Consequence: these three issues remain live on production until the relevant surface (auth screens; checkout) ships from `redesign` to `main` — not a fast fix. Each fix still gets its own `ui-implementer`/`ui-verifier` task rather than being silently folded into a visual-only redesign task, since these are behavior changes, not styling. The InstaPay fix specifically must key off `Partner.id`, not `Partner.name`, when the auth/checkout Phase 4 tasks reach it.
- **OTP is being removed, not wired in.** Login/register/forgot-password stay password-only. Delete the unused OTP-for-registration path (`app/api/auth/otp/*`, `lib/auth/otp.ts`'s registration flow, the always-broken 22-country phone dropdown that only ever validates Egyptian numbers, and any now-dead Redis OTP rate-limit keys tied to registration) during the `(auth)` surface's Phase 4 task — this is a deletion task, verified by `ui-verifier` confirming login/register still work end-to-end and that nothing else depends on the removed endpoints. Forgot-password's OTP flow (the one actually in use) is unaffected.
- **Real search ships in v2.** Already an approved item in `02-proposals.md` ("Postgres full-text search for product search") — Phase 0 just confirmed there's truly nothing to preserve at `/search` today (it's an empty stub), so this is a clean net-new build, not a migration. Scheduled into Phase 4/5 once the storefront surface comes up.
- **The senior-citizen discount feature is removed entirely, not just left un-rebuilt as an accessibility variant.** User's explicit call: "remove the senior accessibility completely from everywhere." This means Phase 4's `(public)` work removes `/profile/senior` and its route/form, the `User.seniorVerified` field's read/write paths, the associated "buy 2 get the cheapest of every 3 free" promo logic wherever it's checked (PDP/cart/checkout pricing), and its admin on/off toggle (wherever that lives — confirm exact location, likely `admin/settings` or `admin/coupons`, before removing) — a full feature removal, not a parity-preservation case. Since this is a live promo mechanic, not dead code, this needs its own reviewed task (with a deliberate look at whether any customer currently holds `seniorVerified = true` and what if anything that implies at removal time) rather than being treated as incidental cleanup. General accessibility (WCAG AA, focus-visible, mobile-first) remains a baseline requirement for everyone per `01-design-system.md` — this decision only removes the age-gated discount feature, not accessibility standards.

## 2026-09-10 — Remaining Phase 0 findings, decided under PM latitude

Lower-ambiguity items decided by the PM rather than put to the user, per the standing "full latitude" grant — noted here so they can be challenged if the user disagrees, rather than silently assumed.

- **`PAYMOB` stays dormant.** It's fully wired server-side as a payment method with zero UI exposure. No evidence of business intent to launch it and no user request to add it — leave the backend as-is, don't build UI for it in v2, don't delete it either (removing a working integration on spec is more churn than leaving inert code that costs nothing to keep). Revisit only if there's an actual business reason to offer a third payment method later.
- **Guest checkout is proposed as a new v2 feature, not decided yet.** Checkout currently hard-requires login (client-JS-gated, no server-side route guard, itself worth tightening while touched). Removing the login requirement is a common, well-evidenced conversion improvement for ecommerce and fits the "propose freely" mandate — but it changes session/order-ownership logic materially, so it's added to `02-proposals.md` as a proposed feature for explicit approval when Phase 4/5 reaches checkout, not silently built.
- **Admin partner-inventory's KPI cards get fixed to true totals.** They currently sum only the loaded pagination page, which reads as a store-wide total to ops staff but isn't one — this is a plain bug (misleading data, not a design choice), fixed as part of that screen's Phase 4 task via a small server-aggregation query, no user sign-off needed.

## 2026-09-09 — Gap-analysis additions

Found during a PM self-review of the plan; folded in per user approval.

- **Database branching**: every new feature we've approved (wishlist, reviews, notification center, `AdminActionLog`) needs Prisma schema changes. Decision: mirror the git branch with a Neon database branch — Neon supports free copy-on-write branching (confirmed 2026-09-09: up to 10 branches/project on the free tier, ~$1.50/branch-month beyond that). All `redesign`-phase migrations run against a `redesign` Neon branch, never the production database. **Not** using `.env.local` for this — that file isn't tied to the git branch (it's just a file on disk) and would silently keep pointing local `main` work at the redesign database too. Instead: a separate `.env.redesign` (gitignored, template committed as `.env.redesign.example`) loaded only via dedicated npm scripts — `dev:redesign`, `db:push:redesign`, `db:migrate:redesign`, `db:studio:redesign` (via `dotenv-cli`, added as a devDependency). Regular `npm run dev`/`db:push`/etc. are untouched and still point at production via `.env`. **Manual step required**: this session has no Neon API credentials, so the branch itself must be created by the user — in the Neon console, or `neonctl branches create --name redesign`; then fill `.env.redesign` from that branch's connection details (pooled → `DATABASE_URL`, direct/unpooled → `DIRECT_URL`). At cutover, either promote the branch to primary or replay its migration history onto production via `prisma migrate deploy`.
- **Existing `DIRECT_URL` note** (found while checking env config, not urgent): production's `.env` currently has `DATABASE_URL` and `DIRECT_URL` set to the same pooled (`-pooler`) connection string. Prisma's documented pattern for Neon is `DIRECT_URL` = the unpooled endpoint (no `-pooler`), used only for migrations. It's likely working today because Neon's pooler generally tolerates DDL, but it's not the supported configuration — worth fixing when convenient, not a redesign blocker. Get the new `redesign` branch's connection strings right from the start (see above) rather than carrying this forward.
- **Preview/checkpoint visibility**: since `git.deploymentEnabled` blocks automatic Vercel builds for `redesign`, define explicit moments where a preview is worth the (small) cost: end of Phase 1's design demo, end of each Phase 4 surface, and just before any merge to `main`. Use `vercel deploy` from the CLI on demand at those points (doesn't require touching the git-deploy gate at all) rather than flipping `deploymentEnabled` back and forth.
- **Rollout & rollback**: cutover granularity stays per-surface (not per-screen) — deliberately, not an oversight: shipping some redesigned screens and some old ones within the same surface would look broken to a real customer. Rollback if a merged surface breaks in production: `git revert` the merge commit on `main` (fast, since Vercel redeploys `main` automatically on push) — a full feature-flag system is unwarranted overhead for a single-tenant app at this scale given the branch model already keeps unfinished work fully out of production until a deliberate merge.
- **Extra scrutiny on checkout/payment tasks**: checkout, cart, and payment-method (COD/InstaPay) screens carry direct revenue risk, more than any other screen. `ui-verifier` treats these as high-stakes (see its updated instructions) — its own pass is not treated as sufficient sign-off on its own; the PM does a manual walkthrough too before merging a checkout-touching task into `redesign`.
- **Route/URL stability**: the public storefront's route structure (`app/(public)/...` slugs/paths) does not change during the rebuild unless a route change is explicitly approved as its own decision — preserving SEO ranking is a parity requirement, not just a technical nice-to-have. Redirects are required for any route that does change.
- **Accessibility and mobile-first are explicit design-system requirements**, not implied ones — see `01-design-system.md`.
- **Error tracking**: add Sentry (free "Developer" tier: 5K errors/month, free forever, but capped at 1 user — fine for this session's usage, revisit if the team grows) during Phase 3 (foundation build) so post-launch regressions surface automatically instead of relying only on user reports.
- **Regression coverage persists past verification**: `ui-verifier`'s manual walkthrough only proves a screen works at the moment it's checked — nothing currently stops a later, unrelated task from silently breaking it. Add a minimal Playwright smoke test per verified screen (happy path + the screen's key states) during Phase 3 setup, so `npm run test` catches future regressions instead of relying on re-verification.

## 2026-09-09 — Branching & versioning

- **Branching**: all redesign work happens off a long-lived `redesign` branch, created from `main` at the tag `pre-redesign-baseline`. `main` stays production and keeps shipping urgent fixes independently. Every `ui-implementer` task branches off `redesign` (not `main`) and PRs back into `redesign`. Periodically merge `main` → `redesign` to absorb production fixes and avoid a painful merge at the end. `redesign` only merges into `main` when a surface (or the whole redesign) is verified ready to ship — no direct-to-`main` merges from task branches.
- **Vercel deployment cost**: `vercel.json#git.deploymentEnabled` explicitly disables auto-deployment for `redesign` and every `redesign/*` task branch (minimatch pattern), while leaving `main` enabled — confirmed via Vercel's own docs (`git.deploymentEnabled`, see https://vercel.com/docs/project-configuration/git-configuration#git.deploymentenabled). Vercel evaluates the `vercel.json` in the commit being pushed, so this takes effect the moment it's committed to `redesign` and is inherited by every task branch created from it — no push, PR, or task-branch build will consume Vercel build minutes until we deliberately flip it back. To preview the redesign before merging to `main`, change the `"redesign": false` entry to `true` (or remove it) temporarily, or just deploy locally / preview via `vercel --local-config` / `npm run build` locally instead of relying on a hosted preview.
- **Versioning**: `package.json#version` is the single source of truth (no other file references it). `main`'s `package.json` still reads `0.1.0`, but the live production app is functionally the platform's first real version — so the redesign is versioned as the product's **v2**, not folded into a belated "1.0". The `redesign` branch starts flat at **`2.0.0`** (no prerelease suffix, per user 2026-09-09) and auto-increments from there:
  - **`npm run version:task`** — patch bump (`2.0.0` → `2.0.1`), run once a task is implemented, verified (APPROVE from `ui-verifier`), and merged into `redesign`. "Automatic" here means: a standardized command the PM runs at that exact checkpoint every time, not a git hook — there's no CI pipeline wired to this repo to trigger it unprompted.
  - **`npm run version:feature`** — minor bump (`2.x.y` → `2.(x+1).0`), run once a *major feature* ships — a full surface rebuild (e.g. all of Phase 4's Auth screens) or an approved item from `02-proposals.md` (e.g. wishlist, reviews). Not every `03-backlog.md` phase warrants this — infrastructure-only phases (e.g. Phase 2's baseline measurement) don't ship a user-facing feature, so they don't get a minor bump on their own.
  - Both use `npm version`, which updates `package.json` + `package-lock.json`, commits, and tags in one step.
  - When the redesign merges to `main`, whatever `2.x.y` it has reached at that point ships as-is — no renormalizing back to a round number.
  - (Optional follow-up, not done yet: formally re-tagging current `main` as `1.0.0` to make the v1/v2 framing explicit in the version history — ask before touching `main`'s version.)
- A tag `pre-redesign-baseline` marks the exact `main` commit the redesign diverged from, for reference/rollback.

## 2026-09-09 — Setup

- **Scope**: all four surfaces are in play — `(public)` storefront, `(admin)` dashboard, `(partner)` portal, `(auth)` pages.
- **Orchestration**: the main session acts as PM — breaks work into backlog tasks, dispatches `ui-implementer` and `ui-verifier` subagents per task, reviews the verifier's report, closes or bounces the task. No separate sessions, no autonomous scheduled loop for now.
- **Model tier**: both `ui-implementer` and `ui-verifier` run on Sonnet. Chosen over a cheaper implementer tier for quality/consistency on a clean-slate rebuild; revisit per-phase if the backlog proves repetitive enough for a cheaper tier once the pattern is proven.
- **Design basis**: clean-slate visual identity, built in Claude Design (see `01-design-system.md`), not an evolution of current styling. Functionality must still match `00-feature-inventory/` exactly unless a task is explicitly sourced from an approved item in `02-proposals.md`.
- **PM latitude**: full — the PM (this session) proposes new features and library/infra swaps freely; the user approves/rejects per item. See `02-proposals.md` for the current approved list.
- **Infra**: open to swapping providers (hosting, DB tier, CDN, image host, etc.), but only after baseline usage is measured (Phase 2) — no swap on instinct alone.
- **Redis (Upstash)**: currently free-tier / unpaid (confirmed by user 2026-09-09). REST-based (`lib/redis.ts`), already used for OTP rate limiting (`lib/redis/otp-limits.ts`) and analytics caching (`lib/cache/analytics.ts`). Free tier is 500K commands/mo + 10GB bandwidth/mo + 256MB storage, and is permanent (not a trial) — confirmed via Upstash's pricing page 2026-09-09. Decision: stay on free tier for now — a single-market (Egypt) storefront at this stage is very unlikely to hit 500K commands/month even after Phase 6 adds more caching (category lists, recommendations, routing lookups). Don't pre-pay "just in case": Upstash's pay-as-you-go overage is $0.20 per 100K commands with no fixed-plan cliff, so even meaningfully exceeding the free tier costs about $1/month, not a scary jump. Check actual usage against the free-tier ceiling as part of Phase 2 (infra baseline) using the Upstash console, and again after Phase 6's caching work lands.
- **Component base**: `components/ui` already follows the shadcn/ui pattern (CVA + clsx + tailwind-merge + Radix primitives: badge, button, card, dialog, input, label, select, table, toast, toaster). Decision: expand this set rather than adopt a different component library — add the missing Radix primitives (dropdown-menu, tabs, popover, tooltip, checkbox, radio-group, switch, form) as needed per screen.
- **Forms**: standardize on `react-hook-form` + the existing Zod schemas (already used server-side) via `@hookform/resolvers/zod`, instead of ad hoc form state.
- **Admin/partner data**: standardize on TanStack Table for data-grid screens and TanStack Query for client-side fetching/caching in admin + partner portals.
